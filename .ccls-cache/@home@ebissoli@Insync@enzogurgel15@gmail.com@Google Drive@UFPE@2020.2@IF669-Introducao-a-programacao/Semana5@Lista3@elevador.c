#include <stdio.h>
int main(){
    int zAndares, nSolicitacoes, eCapacidade, mGenteAndar;
    scanf("%d, %d, %d", &zAndares, &eCapacidade, &nSolicitacoes);
    int mGenteAndar[zAndares][32000];
    while(nSolicitacoes--) scanf("%d, %d", )
    return 0;
}
